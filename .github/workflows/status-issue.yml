---
name: Status Issue Updater (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "This workflow has been deprecated."
          echo "Issue management is now handled by the new 'Issue Management' workflow."
          echo "See: .github/workflows/issue-management.yml"